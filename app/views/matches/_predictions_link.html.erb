<%
  label = if current_player
            if match.players.include?(current_player) || match.predictions.include?(current_player.predictions.find { |p| p.match_id == match.id })
              "Prognóza #{match.predictions_text}"
            else
              "Tipni víťaza"
            end
          else
            "Tipni víťaza"
          end
%>

<% if label %>
  <%= link_to label, match_path(match, anchor: "predictions"), class: "default-color" %>
<% end %>
