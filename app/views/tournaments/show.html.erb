<section class="<%= @tournament.color_base_css %>">
  <h1 class="text-6xl p-20 primary-color text-center">
    <%= @tournament.name %>
  </h1>
  <p class="default-color p-4 md:text-center">
    <%= @tournament.begin_date.to_s %>
    <% if @tournament.end_date > @tournament.begin_date %>
      -
      <%= @tournament.end_date.to_s %>
    <% end %>
  </p>
</section>

<section class="p-2">
  <%= link_to "Pavúk", "#", class: "button button-blue" %>
  <%= link_to "Rozvrh", "#", class: "button button-blue" %>
</section>

<section class="flex flex-wrap p-2 md:px-4">
  <p class="pt-4 w-full md:w-1/3">
    <%= @tournament.main_info.gsub("\n", "<br>").html_safe %>
  </p>
  <p class="pt-4 w-full flex-grow md:w-auto text-justify">
    <%= @tournament.side_info %>
  </p>
</section>
